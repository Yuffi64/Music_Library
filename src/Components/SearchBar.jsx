import React from "react";


const SearchBar = (props) => {
    <div>
        <form>
            <input type = 'search' value={''}/>
        </form>
            {props.parentMusic.filter((music,index))}
    </div>
}